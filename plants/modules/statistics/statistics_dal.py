from __future__ import annotations

from plants.shared.base_dal import BaseDAL


class StatisticsDAL(BaseDAL):
    """Todo currently only a placeholder."""
    # async def get_pollinations(self) -> any:   # todo list[Settings]:
    #     query = select(Pollination)
    #     settings: list[Settings] = list((await self.session.scalars(query)).all())
    #     return settings
