# import logging
#
# from app.app import create_app
# from app.util.logger_utils import configure_root_logger
#
#
# configure_root_logger()
# logging.getLogger(__name__).info('Creating App.')
# app = create_app()
#
# if __name__ == "__main__":
#     app.run(debug=False)
#     logging.getLogger(__name__).info('Starting App.')
